2(5, 8, 12)

5. 设计一个算法从有序顺序表中删除重复的元素，多个值相同的元素仅保留第一个。
8. 有两个递增有序的整数顺序表A, B，设计一个算法将它们中的全部元素放到顺序表C中，要求C这的元素是递减有序的。
12. 
有一个整数序列才用带头结点的单链表L存储。设计一个算法，删除单链表L中data值大于或等于min且小于或等于max的结点（弱表中有这样的结点），同时释放被删结点的空间。
